// import Foundation

enum DataObjectType: String, CaseIterable {
    case account, transaction, group, change//, dashboarditem, project, transaction
//    static let all = ["account", "transaction"] //, "accountgroup", "dashboarditem", "project", "transaction"]
}
